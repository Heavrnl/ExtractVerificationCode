# 是否启用本地正则匹配提取验证码（启用后会优先使用本地匹配，失败后再尝试API）
USE_LOCAL=false

# Prompt模板
PROMPT_TEMPLATE=从以下文本中提取验证码。只输出验证码，不要有任何其他文字。如果没有验证码，只输出'None'。\n\n文本：{input_text}\n\n验证码：

# OpenAI Compatible API Configuration
# Base URL for the API (e.g., https://api.openai.com/v1, https://models.inference.ai.azure.com, etc.)
OPENAI_BASE_URL=https://models.inference.ai.azure.com
# API Key
OPENAI_API_KEY=
# Model Name (e.g., gpt-4o-mini, deepseek-chat, etc.)
OPENAI_MODEL=gpt-4o-mini


# 剪贴板同步配置
SYNC_URL=your_sync_url
SYNC_USERNAME=your_username
SYNC_TOKEN=your_token

# 是否开启调试模式（true/false）
DEBUG_MODE=false
